const removeSilverBox = {
    configName: "removeSilverBox",
    defaultValue: "",
    type: "string",
    explanation: `Remove previous silverBoxes. It can take "first", "last", "all", "number of the SilverBox" (like "'1'").`,
};

export default removeSilverBox;
